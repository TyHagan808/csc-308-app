{
  "name": "csc-308-app",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev -w react-frontend\" \"npm run dev -w express-backend\"",
    "build": "npm run build -w react-frontend",
    "preview": "npm run preview -w react-frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
